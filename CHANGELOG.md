# Changelog

## 0.3.4 - 2026-08-17

- 直接传入 `.doc` 时明确提示先另存为 `.docx`，不再当成损坏压缩包

## 0.3.3 - 2026-08-13

- **脚注/尾注按可见编号定位**："脚注2 · 第1段" 取代跨整个脚注部件的段号；Word 用于分隔线的占位脚注不再计入
- **替换合并要求作者已知**：两条都缺作者信息的相邻删除+插入（畸形文档）不再被误判为"同一作者"而合并
- 深度嵌套的恶意文档给出友好报错而非崩溃；`--author` 过滤忽略首尾空格（Word 有时存储带尾随空格的作者名）
- 示例文档补全 Content-Types 与关系声明（可在 Word/WPS 中直接打开），并新增脚注批注与格式修订演示；README 示例同步刷新
- 工程化：接入 ruff（含 CI lint 关卡），新增畸形输入与随机文档的健壮性测试（81 个测试）

## 0.3.2 - 2026-08-13

- **格式合并更精确**：同段落里两处独立的格式修订（中间隔着普通文字）不再被误并成一行；只合并同一次操作被 Word 拆碎的相邻 run（与替换合并共用 run 时钟相邻性判定）
- **Windows 重定向输出 UTF-8**：`docx-reply x.docx > out.md` 在中文 Windows 上此前会按 GBK 写文件，现统一输出 UTF-8，任何编辑器打开不乱码
- 多个页眉/页脚文件按自然序处理（`header2.xml` 先于 `header10.xml`）
- 批注范围缺失终点标记（畸形文档）时，原文摘录按"起点到部件末尾"兜底，不再静默为空
- 不间断连字符（`w:noBreakHyphen`）保留为 `-`

## 0.3.1 - 2026-08-13

提取保真度修复（对抗性审查轮）：

- **文本框**：`w:drawing` / `w:pict` / `mc:AlternateContent` 里的批注与修订此前被整体丢弃，现在正常提取；Word 同时写入的 `mc:Choice` + `mc:Fallback` 双重表示只取其一，不重复计数
- **公式**：OMML 数学文本（`m:t`）此前丢失——删除一条公式的修订会因内容为空整条消失，现以线性文本提取（如 `E=mc²`）；批注内容里的公式同样生效
- **被删文字的原文摘录**：批注框选的文字被审阅删除时，"原文摘录"不再丢失被删部分——摘录按批注时的原文给出，段落文本仍按修改后结果
- **注音（ruby）**：只保留基字，注音标注不再混入正文
- **docx 对照表**：补上 OOXML 规范要求的 `w:tblGrid`，避免严格实现报"内容不可读"

## 0.3.0 - 2026-08-13

- **全文档范围**：脚注、尾注、页眉、页脚中的批注与修订同样提取，位置列自动带上"脚注 / 页眉"等前缀，JSON 新增 `part` 字段
- **移动修订**：拖动/剪切粘贴产生的 `moveTo` 以"移动"类型列出一次（新位置），`moveFrom` 不再重复
- **格式修订**：加粗、字号、段落格式等变更（`rPrChange` / `pPrChange`）以"格式"类型列出被改动的文字；同一段落内连续的格式 run 自动合并为一行
- 替换合并在有格式修订穿插时依旧正确配对

## 0.2.0 - 2026-08-13

- **Word 版对照表输出**：`-f docx -o 修改对照表.docx` 直接生成可填写提交的 Word 表格
- **替换合并**：同一作者紧邻的删除+插入（Word 里"选中改写"的产物）合并为一条"替换：旧词 → 新词"（JSON 中 `kind: "replace"`，含 `deleted` / `inserted` 字段）
- **标题定位**：位置列升级为"最近标题 · 第 N 段"（识别 Word/WPS 内置标题样式），JSON 新增 `heading` 字段
- **过滤器**：`--author 姓名`（可重复）只保留指定审阅人；`--skip-resolved` 跳过已解决批注
- `-f docx` 未指定 `-o` 时给出明确错误提示

## 0.1.0 - 2026-08-13

首个版本。

- 解析 `.docx` 批注：批注人、日期、批注内容、被框选的原文、所在段落
- 回复线程与"已解决"状态（`commentsExtended.xml`），主批注按文档位置排序
- 修订记录：插入（`w:ins`）与删除（`w:del`），移动的文字（moveFrom/moveTo）在正文中正确呈现
- 输出：Markdown 修改对照表（默认，带空白"修改说明"列）、CSV（UTF-8 BOM，Excel/WPS 友好，修订另存 `.revisions.csv`）、JSON
- 纯标准库实现，零第三方依赖；Windows 控制台编码安全
- 支持批注跨段落、表格内批注、无范围的点批注
